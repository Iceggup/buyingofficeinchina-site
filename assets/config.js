/* ============================================================
   SINGLE PLACE TO EDIT CONTACT DETAILS
   ------------------------------------------------------------
   Change the four values below and the whole site updates:
   every WhatsApp link, every email link, every CTA.
   (Brand name is also baked into the page <title> tags for SEO —
    to rename the brand, run  ../tools/apply_brand.py  instead.)
   ============================================================ */
window.SITE = {
  brand: "Buying Office in China",

  /* WhatsApp number in full international format, digits only.
     No +, no spaces, no brackets. */
  whatsapp: "85252072721",

  /* WhatsApp pre-filled message shown when a visitor taps the button */
  whatsappMessage: "Hello, I would like to send a procurement requirement for China sourcing.",

  /* Where inquiry notifications are sent. Leave blank to disable. */
  email: "iceggup@gmail.com",

  /* Used in sitemap / canonical / OG tags. No trailing slash. */
  domain: "https://buyingofficeinchina.com"
};
